# take inputs
# assign inputs to variables at the end of each template file
# f.open('a') or whatever in each python template file what you want to assign the column names to be
# this way each python file will 